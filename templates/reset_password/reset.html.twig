{% extends 'base_content.html.twig' %}

{% block title %}Сбросьте свой пароль{% endblock %}

{% block content %}
<div class="Section">
    <div class="wrap">
        {{ form_start(resetForm) }}
            {{ form_row(resetForm.plainPassword) }}
            <button class="btn btn-primary">Reset password</button>
        {{ form_end(resetForm) }}
    </div>
</div>
{% endblock %}
